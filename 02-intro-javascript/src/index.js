// Operador condicional ternario

const activo = true;
let mensaje = activo ? 'activo' : 'inactivo';

// if (activo) {
//     mensaje = 'activo';
// } else {
//     mensaje = 'inactivo';
// }

console.log(mensaje);